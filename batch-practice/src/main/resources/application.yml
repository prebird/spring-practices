spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3316/batch
    username: root
    password: 12

  batch:
    job:
      name: simpleJob

logging:
  level:
    org.springframework.batch: DEBUG
